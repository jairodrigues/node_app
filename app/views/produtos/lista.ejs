<html>
    <body>
        <table>
            <thead>
            <tr>
                <td>id</td>
                <td>nome</td>
                <td>Preco</td>
            </tr>
            </thead>
            <tbody>
            <% for (var i=0; i<lista.length; i++){ %>
                <tr>
                    <td><%= lista[i].id %></td>
                    <td><%= lista[i].titulo %></td>
                    <td><%= lista[i].preco %></td>
                    <td><a href="/produtos/<%= lista[i].id%>">Excluir</a>
                </tr>
            <% } %>
            </tbody>
        </table>
       </body>
</html>
